<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
<!--    <uses-permissidn android:name="android.permission.MANAGE_EXTERNAL_STORAGE"/>-->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CoviVacciCare1">
        <activity android:name=".SearchActivity" />
        <activity android:name=".PasschngActivity" />
        <activity android:name=".AdminviewActivity" />
        <activity android:name=".AdmindashboardActivity" />
        <activity android:name=".RecyclerActivity" />
        <activity android:name=".EditActivity" />
        <activity android:name=".AdminloginActivity" /> <!-- <activity -->
        <!-- android:name=".ui.login.AdminloginActivity" -->
        <!-- android:label="@string/title_activity_adminlogin" /> -->
        <activity android:name=".DesignationActivity" />
        <activity android:name=".ViewdetailsActivity" />
        <activity android:name=".DashboardActivity" />
        <activity android:name=".RegisterActivity" />
        <activity
            android:name=".VerifyotpActivity"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.CoviVacciCare1"
            android:windowSoftInputMode="adjustResize" />
        <activity
            android:name=".SendotpActivity"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.CoviVacciCare1"
            android:windowSoftInputMode="adjustResize" />
        <activity
            android:name=".MainActivity"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.CoviVacciCare1"
            android:windowSoftInputMode="adjustResize"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>